# GitLab Pages deployment configuration
image: node:20

# Cache node_modules for faster builds
cache:
  paths:
    - node_modules/

# Only run on main branch or tags
pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - rm -rf public
    - mv dist public  # GitLab Pages expects the 'public' directory
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_TAG
